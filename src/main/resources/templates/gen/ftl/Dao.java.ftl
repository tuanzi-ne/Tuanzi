package ${package!}.dao;

import ${package!}.pojo.${className!}Info;
import com.spring.common.mvc.BaseDao;
import org.springframework.stereotype.Repository;

/**
 * ${comments!}
 *
 * @author ${author!}
 * @date ${datetime!}
 * @since V1.0
 */
@Repository
public interface ${className!}Dao extends BaseDao<${className!}Info> {
	
}
